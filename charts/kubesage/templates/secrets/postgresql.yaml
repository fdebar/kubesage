---
apiVersion: v1
kind: Secret
metadata:
  name:  {{ include "kubesage.fullname" . }}-postgresql-secret
type: Opaque
stringData:
  DATABASE_USER: {{ .Values.postgresql.auth.username | quote }}
  DATABASE_PASSWORD: {{ .Values.postgresql.auth.password | quote }}