---
apiVersion: v1
kind: Secret
metadata:
  name:  {{ include "kubesage.fullname" . }}-secret
type: Opaque
stringData:
  DATABASE_PASSWORD: {{ .Values.postgresql.auth.password | default "" | quote }}
  OPENAI_API_KEY: {{ .Values.openai.apiKey | default "" | quote }}
