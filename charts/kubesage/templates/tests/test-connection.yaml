apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubesage.fullname" . }}-test-connection"
  labels:
    {{- include "kubesage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubesage.fullname" . }}:{{ .Values.api.service.port }}']
  restartPolicy: Never
