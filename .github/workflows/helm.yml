name: KubeSage Helm Chart

permissions:
  contents: read

on:
  pull_request:
    branches:
      - main
    paths:
      - "charts/**"
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"


jobs:
  helm-test:
    name: Helm Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Helm
        uses: azure/setup-helm@v5

      - name: Lint Helm chart
        run: make helm-lint

      - name: Build Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: false
          tags: |
            kubesage:helm-test

      - name: Create Kind cluster
        uses: helm/kind-action@v1.14.0
        with:
          cluster_name: kind-kubesage

      - name: Load Docker image into Kind
        run: kind load docker-image kubesage:helm-test --name kind-kubesage

      - name: Add Helm repositories
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo update

      - name: Deploy PostgreSQL
        run: |
          helm upgrade --install postgresql bitnami/postgresql \
            --namespace kubesage \
            --create-namespace \
            --set fullnameOverride=kubesage-postgresql \
            --set auth.username=kubesage \
            --set auth.database=kubesage \
            --set auth.password=kubesage-dev \
            --set auth.postgresPassword=kubesage-postgres-dev \
            --set primary.persistence.enabled=false

      - name: Wait for PostgreSQL
        run: |
          kubectl rollout status statefulset/kubesage-postgresql \
            -n kubesage \
            --timeout=360s

      - name: Deploy Helm chart
        run: |
          helm dependency build charts/kubesage
          helm upgrade --install kubesage ./charts/kubesage \
            --namespace kubesage \
            --create-namespace \
            --set api.image.repository=kubesage \
            --set api.image.tag=helm-test \
            --set api.resources.requests.memory=56Mi \
            --set api.resources.requests.cpu=50m \
            --set worker.image.repository=kubesage \
            --set worker.image.tag=helm-test \
            --set worker.resources.requests.memory=56Mi \
            --set worker.resources.requests.cpu=50m \
            --set ai.apiKey=ollama \
            --set ai.provider=ollama \
            --set database.host=kubesage-postgresql \
            --set database.port=5432 \
            --set database.name=kubesage \
            --set database.username=kubesage \
            --set database.existingSecret=kubesage-postgresql \
            --set database.passwordKey=password

      - name: Verify deployment
        run: |
          kubectl rollout status deployment/kubesage-api \
            -n kubesage \
            --timeout=360s

      - name: Verify worker deployment
        run: |
          kubectl rollout status deployment/kubesage-worker \
            -n kubesage \
            --timeout=360s

      - name: Dump logs if failed
        if: failure()
        run: |
          kubectl get all -n kubesage
          kubectl get jobs -n kubesage
          kubectl describe job kubesage-migration -n kubesage
          kubectl logs job/kubesage-migration -n kubesage --all-containers=true || true
          kubectl describe statefulset kubesage-postgresql -n kubesage
          kubectl logs statefulset/kubesage-postgresql -n kubesage --all-containers=true || true
          kubectl describe deployment/kubesage-api -n kubesage
          kubectl logs deployment/kubesage-api -n kubesage --all-containers=true || true
          kubectl describe deployment/kubesage-worker -n kubesage
          kubectl logs deployment/kubesage-worker -n kubesage --all-containers=true || true

  publish:
    name: Publish Helm Chart
    needs: helm-test
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Helm
        uses: azure/setup-helm@v5

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" |
            helm registry login ghcr.io \
              -u "${{ github.actor }}" \
              --password-stdin

      - name: Build Helm dependencies
        run: |
          helm repo add postgresql https://charts.bitnami.com/bitnami
          helm repo update
          helm dependency build ./charts/kubesage

      - name: Package Helm chart
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir -p dist
          helm package ./charts/kubesage \
            --version "${VERSION}" \
            --app-version "${VERSION}" \
            --destination dist

      - name: Push Helm chart
        run: |
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          helm push dist/kubesage-*.tgz oci://ghcr.io/${OWNER}/charts