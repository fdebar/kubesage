---
apiVersion: v1
kind: Pod
metadata:
  name: kubesage-restart-loop
  namespace: default
spec:
  containers:
    - name: restart
      image: busybox
      command:
        - sh
        - -c
        - |
          sleep 5
          exit 1
