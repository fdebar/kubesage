---
apiVersion: v1
kind: Service
metadata:
  name: kubesage
  namespace: kubesage
  labels:
    app: kubesage
spec:
  selector:
    app: kubesage
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
